[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "xgic-cli"
version = "0.2.0"
description = "XGIC CLI — modular command-line framework for XGIC tools and environment orchestration (core package)."
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [
    { name = "XGIC" }
]
requires-python = ">=3.14"
dependencies = [
    "rich>=13.7",
]
keywords = [
    "xgic",
    "cli",
    "framework",
    "automation",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]

[project.urls]
Homepage = "https://github.com/xgic/cli"
Repository = "https://github.com/xgic/cli"
Issues = "https://github.com/xgic/cli/issues"
Documentation = "https://github.com/xgic/cli#readme"
Changelog = "https://github.com/xgic/cli/releases"

[project.scripts]
xgic = "xgic.cli.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
]
test = [
    "pytest>=8.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/xgic"]

[tool.hatch.build.targets.sdist]
include = [
    "src/",
    "README.md",
    "LICENSE",
    "NOTICE",
    "pyproject.toml",
]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["xgic"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
